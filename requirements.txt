# NADiSSP — Python dependencies
# Backend: sklearn (CPU, no PyTorch required for edge deployment)
numpy>=1.24
pandas>=1.5
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
flask>=3.0
gunicorn>=23.0
# For TCO simulation sensitivity analysis
# scipy already listed above

# Optional — for ONNX export of sklearn models to GPU deployment:
# onnxruntime>=1.16
# skl2onnx>=1.16

# Dev / notebook extras (not needed at runtime):
# jupyter
# ipykernel
